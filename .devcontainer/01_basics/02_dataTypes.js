"use strict"; // treate all code as a newer version like class, module, arrow function
// alert(3+3); // alert not defined - we are using the node not browser

console.log(3+
    3
); // code readability should be high
console.log("Raju");

// ECMA SCript - Organisation to set Standard of code like if, for how it should work   
// https://tc39.es/ecma262/

// https://developer.mozilla.org/en-US/docs/Web/JavaScript

// Datatypes:

// 1. Primitive Data Types:

// Number

let age =23;
// String
let name ="Raju Jadhav";
let fullName = 'raju gopal jadhav';

// Boolean
let isLoggedIn = true;
let isLoggedOut = false;

// Null - stanalone value , Means empty value

// Undefined - Value not assigned \

let state;

// Symbol - Use when unique representation

// 2. Non-premitive data types

// 60% discharge of laptop = 2.5 hr = 150 min =>= 150min/60% => 2.5min - 1% ;
//  100% - 2.5*100 = 250 min => 4.16 hr backup

console.log(typeof "Hitesh");
console.log(typeof age);
console.log(typeof null);  // null is an object type - people saying it's bug
console.log(typeof undefined); // undefinde is undefined type itselft